name = "tiye/dom-ffi"

version = "0.5.0"

readme = "README.md"

repository = "https://github.com/Respo/dom-ffi.mbt"

license = "Apache-2.0"

keywords = [ "dom", "ffi" ]

description = "Typed MoonBit bindings for browser DOM APIs"

source = "src"

preferred_target = "js"

options(
  homepage: "https://github.com/Respo/dom-ffi.mbt",
)
